import { Meta } from '@storybook/react';
import { MiniCalendar } from './MiniCalendar';

const meta: Meta<typeof MiniCalendar> = {
  component: MiniCalendar,
  title: 'MiniCalendar',
};

export default meta;

export const BaseMiniCalendar = {};
